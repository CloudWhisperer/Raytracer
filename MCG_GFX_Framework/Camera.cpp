#include "Camera.h"
#include <map>
using namespace std;
#include <iostream>

ray camera::camray(glm::vec2 _pixelpos)
{
    std::map<float, float> _pixelpos.x;

    const float xnormal = map(static_cast<float>(pixelpos.x),
        0.0f, static_cast<float>(mscreenresolution.y), 1.0f, -1.0f);

    return ray();
}
